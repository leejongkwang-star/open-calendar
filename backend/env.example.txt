# 서버 설정
PORT=3001
NODE_ENV=development


# 데이터베이스 연결 (PostgreSQL)
#
# Supabase 사용 시:
# Settings → Database → Connection string → URI 탭에서 복사
# 형식: postgresql://postgres.[프로젝트-REF]:[비밀번호]@aws-0-[지역].pooler.supabase.com:6543/postgres
#
# 로컬 PostgreSQL 사용 시:
# DATABASE_URL="postgresql://user:password@localhost:5432/calendar_db?schema=public"
DATABASE_URL="postgresql://postgres.[프로젝트-REF]:[비밀번호]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres"

# 마이그레이션 전용 직접 연결 (포트 5432)
# prisma migrate deploy / status 에서만 사용되며, 런타임에는 쓰이지 않는다.
# Supabase: Settings → Database → Connection string → Direct connection
DIRECT_DATABASE_URL="postgresql://postgres:[비밀번호]@db.[프로젝트-REF].supabase.co:5432/postgres"

# JWT 비밀키
# openssl rand -base64 32 로 생성한 값을 사용하고, 부서(배포)별로 서로 다른 값을 쓴다.
# 실제 값은 이 파일에 적지 말고 Cloud Run 환경변수 또는 로컬 .env 에만 둔다.
JWT_SECRET="[openssl rand -base64 32 결과로 교체]"
JWT_EXPIRES_IN="1h"

# CORS 설정 (프론트엔드 origin 과 1:1 로 맞춘다)
CORS_ORIGIN="http://localhost:5173"
