# 백엔드 라우팅 오류 해결: Cannot GET /api/auth/check-employee-number

## 🔴 문제 상황

브라우저에서 직접 API를 호출했을 때:
```
Cannot GET /api/auth/check-employee-number
```

이 오류는 Express가 해당 라우트를 찾을 수 없다는 의미입니다.

## 🔍 원인 분석

코드를 확인한 결과, 라우팅 설정은 올바릅니다:

1. ✅ `server.js`: `app.use('/api/auth', authRoutes)` - 올바름
2. ✅ `auth.js`: `router.get('/check-employee-number', ...)` - 올바름
3. ✅ 최종 경로: `/api/auth/check-employee-number` - 올바름

## 🚨 가능한 원인

### 1. Render에 최신 코드가 배포되지 않음 (가장 가능성 높음)

Render에 배포된 코드가 오래된 버전일 수 있습니다.

**해결 방법:**
1. Render 대시보드 접속
2. 서비스 선택
3. **Manual Deploy** → **Deploy latest commit**
4. 또는 Git에 푸시하면 자동 재배포

### 2. Render 서버가 재시작되지 않음

코드 변경 후 서버가 재시작되지 않았을 수 있습니다.

**해결 방법:**
- Render 대시보드에서 서비스 재시작

### 3. 백엔드 서버가 제대로 실행되지 않음

**확인 방법:**
```
https://your-backend.onrender.com/health
```

이 URL이 작동하면 서버는 실행 중입니다.

## ✅ 해결 방법

### 방법 1: Render에 코드 재배포

1. **로컬에서 변경사항 확인:**
   ```bash
   cd backend
   git status
   git log --oneline -5
   ```

2. **Git에 푸시:**
   ```bash
   git add .
   git commit -m "백엔드 라우팅 확인"
   git push origin main
   ```

3. **Render 자동 재배포 확인**
   - Render 대시보드에서 배포 상태 확인

### 방법 2: Render에서 수동 재배포

1. Render 대시보드 접속
2. 백엔드 서비스 선택
3. **Manual Deploy** 클릭
4. **Deploy latest commit** 선택

### 방법 3: Render 서버 로그 확인

1. Render 대시보드 → 서비스 선택
2. **Logs** 탭 확인
3. 다음을 확인:
   - 서버가 정상 시작되었는지
   - `🚀 서버가 http://localhost:${PORT}에서 실행 중입니다.` 메시지가 있는지
   - 라우팅 관련 에러가 있는지

## 🔧 추가 확인사항

### 1. 다른 API 엔드포인트 테스트

브라우저에서 테스트:
```
https://your-backend.onrender.com/api/auth/check-employee-number?employeeNumber=A99120
```

**실패하면:** 라우팅 문제

다른 엔드포인트도 테스트:
```
https://your-backend.onrender.com/health
```

### 2. 코드 버전 확인

Render에 배포된 코드가 최신인지 확인:

1. Render 대시보드 → **Settings** → **Build & Deploy**
2. **Branch** 확인
3. 최신 커밋 확인

### 3. 라우터 파일 확인

Render에서 실제로 배포된 파일 확인:
- `backend/src/routes/auth.js` 파일에 `check-employee-number` 라우트가 있는지

## 📝 확인 체크리스트

- [ ] Git에 최신 코드가 푸시되었는가?
- [ ] Render에 최신 코드가 배포되었는가?
- [ ] Render 서버가 정상 실행 중인가? (`/health` 확인)
- [ ] Render 로그에 에러가 없는가?
- [ ] `auth.js` 파일에 `check-employee-number` 라우트가 있는가?

## 💡 빠른 해결

1. **백엔드 코드 최신화:**
   ```bash
   cd backend
   git add .
   git commit -m "라우팅 확인"
   git push
   ```

2. **Render 재배포 확인**
   - Render 대시보드에서 자동 배포 확인
   - 또는 Manual Deploy

3. **테스트:**
   ```
   https://your-backend.onrender.com/api/auth/check-employee-number?employeeNumber=A99120
   ```

---

**가장 가능성 높은 원인: Render에 최신 코드가 배포되지 않음**

먼저 Git에 푸시하고 Render에서 재배포를 확인하세요!

